[build-system]
requires = ["scikit-build-core", "mypy"]
build-backend = "scikit_build_core.build"

[project]
name = "c-core-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.20.2",
]
